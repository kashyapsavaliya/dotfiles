#!/bin/bash

# Translates program names to icons for use in configurations

case "$1" in
  ack|fd|find|fzf|grep|rg)
    echo  ;;
  atop|htop|top)
    echo  ;;
  bash|fish|zsh|sh|ash)
    echo  ;;
  bat|cat)
    echo  ;;
  clx)
    echo  ;;
  cp|duplicate)
    echo  ;;
  diff)
    echo  ;;
  mv)
    echo 󰉒 ;;
  curl|http|pint|lynx|wget|w3m)
    echo  ;;
  docker|docker-compose|podman)
    echo  ;;
  lf|ls|nnn|ranger|lsd)
    echo  ;;
  elixir|mix)
    echo  ;;
  gh|gh-dash)
    echo  ;;
  git|lazygit|tig)
    echo  ;;
  glow)
    echo  ;;
  go|goreleaser)
    echo  ;;
  java|mvn)
    echo  ;;
  less|more)
    echo  ;;
  lua)
    echo  ;;
  man)
    echo  ;;
  nano|pico)
    echo  ;;
  node|npm|yarn)
    echo  ;;
  'nix*')
    echo 󱄅 ;;
  vim|vi)
    echo  ;;
  nvim)
    echo  ;;
  python|pip)
    echo  ;;
  rm)
    echo  ;;
  rsync)
    echo  ;;
  ruby|irb)
    echo  ;;
  scp|ssh)
    echo 󰣀 ;;
  sleep)
    echo  ;;
  sudo)
    echo  ;;
  tail)
    echo  ;;
  task|make)
    echo  ;;
  tmux)
    echo  ;;
  youtube-dl)
    echo  ;;
  *)
    echo  ;;  # Default icon
esac

